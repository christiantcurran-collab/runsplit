import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Heart Rate Zone Calculator — RunSplit",
  description: "Calculate your 5 heart rate training zones using the Karvonen method. Know exactly how hard to train.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

