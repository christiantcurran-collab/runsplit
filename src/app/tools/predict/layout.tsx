import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Race Time Predictor — What Can I Run? — RunSplit",
  description: "Predict your race time for any distance from a recent result. Uses Riegel and Cameron formulas. Free tool.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

