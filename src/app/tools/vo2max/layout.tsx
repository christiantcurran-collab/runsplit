import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "VO2max Estimator — RunSplit",
  description: "Estimate your VO2max from any race result using the Jack Daniels formula. See your fitness classification.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }


