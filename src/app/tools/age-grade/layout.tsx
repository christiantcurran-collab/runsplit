import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Age-Graded Running Calculator — RunSplit",
  description: "See how good your time really is. Age-graded performance percentage based on WMA standards.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }


