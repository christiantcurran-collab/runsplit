import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Calories Burned Running Calculator — RunSplit",
  description: "Estimate how many calories you burn running based on distance, weight, and pace. Uses MET values for accuracy.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }


