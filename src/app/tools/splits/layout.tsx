import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Race Split Planner — RunSplit",
  description: "Plan your race splits with even or negative split strategies. Free split calculator for any distance.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }


