import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Find My Training Paces — RunSplit",
  description: "Calculate easy, tempo, interval and repetition paces from a recent race result. Based on Jack Daniels methodology.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

