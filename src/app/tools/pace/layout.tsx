import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Pace & Speed Calculator — RunSplit",
  description: "Calculate your running pace, speed, and equivalent race times for any distance. Free, instant, no signup.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

