import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Pace Converter — min/km, min/mi, km/h, mph — RunSplit",
  description: "Instantly convert between running pace and speed units. min/km, min/mile, km/h, mph, m/s.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }

